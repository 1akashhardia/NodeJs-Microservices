apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservices-app.fullname" . }}-test-connection"
  labels:
    {{- include "microservices-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservices-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
